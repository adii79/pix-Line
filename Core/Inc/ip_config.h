/**
  ******************************************************************************
  * @file    ip_config.h
  * @brief   Legacy shim. All configuration now lives in device_config.h
  *          (struct g_cfg, persisted to flash). This header is kept only so
  *          existing #include "ip_config.h" lines keep compiling.
  ******************************************************************************
  */
#ifndef IP_CONFIG_H
#define IP_CONFIG_H

#include "device_config.h"

#endif /* IP_CONFIG_H */
